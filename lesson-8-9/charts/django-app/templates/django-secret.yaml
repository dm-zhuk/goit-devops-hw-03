apiVersion: v1
kind: Secret
metadata:
  name: {{ include "django-app.fullname" . }}-secret
type: Opaque
data:
  SECRET_KEY: {{ .Values.secretEnv.SECRET_KEY | b64enc | quote }}
  DATABASE_URL: {{ .Values.secretEnv.DATABASE_URL | b64enc | quote }}